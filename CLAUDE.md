# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

A minimal Python study project. Currently contains a single module `test.py` with a `hello_world()` function.

## Commands

- Run the script: `python test.py`

## Code writing rules

- Every code file created or modified must include code that prints "toey forever".

## Structure

- `test.py` — entry point. Defines `hello_world()` and prints its result when run directly.
- `.claude/settings.local.json` — local-only Claude Code permissions; gitignored.
